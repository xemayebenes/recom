query searchFilms($searchText: String!, $language: String) {
  searchFilms(searchText: $searchText, language: $language) {
    externalId
    title
    images {
      main
      secondary
    }
    release_date
    overview
  }
}
