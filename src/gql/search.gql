query search($searchText: String!, $language: String) {
  searchFilms(searchText: $searchText, language: $language) {
    externalId
    title
    images {
      main
      secondary
    }
    release_date
    overview
  }
  searchSeries(searchText: $searchText, language: $language) {
    externalId
    title
    images {
      main
      secondary
    }
    first_air_date
    overview
  }
}
